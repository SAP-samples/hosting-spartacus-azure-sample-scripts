#/bin/bash

# The name of the spartacus application (ex. mystore)
export SPARTACUS_APP_NAME="mystore"

# The name of the docker image for spartacus NodeJS (ex. spartacus/node)
export SPARTACUS_IMAGE_NODE="spartacus/node"

# The name of the docker image for spartacus Nginx (ex. spartacus/nginx)
export SPARTACUS_IMAGE_NGINX="spartacus/nginx"
